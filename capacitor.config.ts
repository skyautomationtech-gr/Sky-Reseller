const config = {
  appId: 'com.skyautomationtech.reseller',
  appName: 'Sky Reseller',
  webDir: 'dist',
  plugins: {
    PushNotifications: {
      presentationOptions: ["badge", "sound", "alert"],
    },
  },
};

export default config;
